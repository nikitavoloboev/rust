{
  "scripts": {
    "dev": "cargo watch -q -- sh -c \"tput reset && cargo run -q\"",
    "test": "cargo watch -q -- sh -c \"tput reset && cargo test -q\"",
    "markdown-test": "cd crates/markdown && cargo watch -q -- sh -c \"tput reset && cargo test -q --lib\"",
    "seed-clone": "git clone https://github.com/learn-anything/seed"
  }
}
